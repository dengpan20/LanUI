# Lan UI 独立项目示例

该目录是独立的 Vue 3 + Vite 消费项目，组件通过 `lan-ui-design-system` 包导入。

```powershell
pnpm install
pnpm --dir examples/standalone-vue dev
```

复制到仓库外部时，把 `package.json` 中的 `file:../..` 替换为组件包版本、工作区协议或实际 tarball 路径。
